import { mode, Styles } from '@chakra-ui/theme-tools'

export const styles: Styles = {
  /*
    Example of overwrite global styles

    global: props => ({
      body: {
        fontFamily: 'body',
        color: mode('gray.800', 'whiteAlpha.900')(props),
        bg: mode('red', 'green')(props),
      },
    }),
  */
}
